apiVersion: v1
kind: Service
metadata:
  name: ping-pong
  labels:
    app.kubernetes.io/name: ping-pong
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ping-pong
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
